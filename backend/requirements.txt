# Voice Agent Backend — pinned versions for reproducibility
# Install: pip install -r requirements.txt (inside a venv)

# Web server (FastAPI 0.115+ for Starlette compatibility with langchain-groq deps)
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Agent & LLM
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-groq>=0.1.0
langchain-core>=0.1.0

# OpenAI (LLM + optional STT/TTS)
openai>=1.12.0

# Env and types
python-dotenv>=1.0.0
